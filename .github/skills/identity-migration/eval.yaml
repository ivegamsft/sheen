name: "identity-migration-routing"
description: "Routing evaluation — validates trigger activation for the identity-migration skill."
skill: "skills/identity-migration/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "We need to migrate our existing ASP.NET Membership user accounts to ASP.NET Core Identity."
    expect_activation: true
  - id: "pos-2"
    input: "How do I preserve legacy password hashes during migration so users don't have to reset passwords?"
    expect_activation: true
  - id: "pos-3"
    input: "I want to integrate Entra ID OIDC into our ASP.NET Core Identity setup alongside local accounts."
    expect_activation: true
  - id: "neg-1"
    input: "Build a login form component in Vue.js for our marketing site."
    expect_activation: false
  - id: "neg-2"
    input: "How do I configure a VPN for network access control?"
    expect_activation: false
