name: "bom-schema-routing"
description: "Routing evaluation — validates trigger activation for the bom-schema skill."
skill: "skills/bom-schema/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Create a JSON schema to validate BOM entries, including required resource IDs and item names."
    expect_activation: true
  - id: "pos-2"
    input: "Review the BOM schema for required cells and naming conventions before publishing it."
    expect_activation: true
  - id: "pos-3"
    input: "Define a validation template for bill-of-materials data with stable identifiers."
    expect_activation: true
  - id: "neg-1"
    input: "Generate a customer-facing sales deck."
    expect_activation: false
  - id: "neg-2"
    input: "Tune a Kubernetes autoscaling policy."
    expect_activation: false
