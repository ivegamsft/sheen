name: "environment-audit-drift-routing"
description: "Routing evaluation for environment-audit-drift skill activation and suppression."
skill: "skills/environment-audit-drift/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Audit environment-map.yml for drift before deployment."
    expect_activation: true
  - id: "pos-2"
    input: "Check whether GitHub branch protection and Azure resources match our environment map."
    expect_activation: true
  - id: "pos-3"
    input: "Generate a drift report with severity and remediation for staging and prod."
    expect_activation: true
  - id: "pos-4"
    input: "Compare release manifest versions to deployed Container Apps revisions and flag ReleaseId tag drift."
    expect_activation: true
  - id: "neg-1"
    input: "Implement a new React component with responsive layout."
    expect_activation: false
  - id: "neg-2"
    input: "Create a new PostgreSQL migration for invoice totals."
    expect_activation: false
