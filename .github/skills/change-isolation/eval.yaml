name: "change-isolation-routing"
description: "Routing evaluation — validates trigger activation for the change-isolation skill."
skill: "skills/change-isolation/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Design path-based workflow isolation so mobile and infra releases can run independently."
    expect_activation: true
  - id: "pos-2"
    input: "Audit our GitHub Actions triggers for cross-layer coupling between portal and database lanes."
    expect_activation: true
  - id: "pos-3"
    input: "Create release lane contracts for independent versioning and rollback by monorepo layer."
    expect_activation: true
  - id: "neg-1"
    input: "Implement a new API endpoint for customer profile search."
    expect_activation: false
  - id: "neg-2"
    input: "Write SQL to backfill missing user records in production."
    expect_activation: false
