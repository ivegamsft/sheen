name: "create-instruction-routing"
description: "Routing evaluation — validates trigger activation for the create-instruction skill."
skill: "skills/create-instruction/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Create a new instructions file that enforces our TypeScript naming conventions."
    expect_activation: true
  - id: "pos-2"
    input: "What applyTo glob should I use for an instruction that only targets Python test files?"
    expect_activation: true
  - id: "pos-3"
    input: "Write the frontmatter and guardrails for a code review workflow instruction."
    expect_activation: true
  - id: "neg-1"
    input: "Build a new reusable skill for containerizing legacy apps."
    expect_activation: false
  - id: "neg-2"
    input: "Write the marketing copy for our product launch page."
    expect_activation: false
