name: "dependency-blocker-monitoring-routing"
description: "Routing evaluation — validates trigger activation for the dependency-blocker-monitoring skill."
skill: "skills/dependency-blocker-monitoring/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Monitor workcell BOM dependencies and cell health so blocker issues open when a cell fails."
    expect_activation: true
  - id: "pos-2"
    input: "Create blocker issues for failed cells and resolve them when the deployment recovers."
    expect_activation: true
  - id: "pos-3"
    input: "Read the BOM dependencies, check cell health, and close the matching blocker issue after recovery."
    expect_activation: true
  - id: "neg-1"
    input: "Design a new landing page."
    expect_activation: false
  - id: "neg-2"
    input: "Write a dependency upgrade guide."
    expect_activation: false
