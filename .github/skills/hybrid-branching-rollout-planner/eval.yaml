name: "hybrid-branching-rollout-planner-routing"
description: "Routing evaluation for hybrid-branching-rollout-planner skill activation."
skill: "skills/hybrid-branching-rollout-planner/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Create a phased rollout plan for hybrid branching with dependency and impact mapping for our main repo and downstream repos."
    expect_activation: true
  - id: "pos-2"
    input: "Plan migration to trunk plus release/hotfix plus agent lanes and include baseline measurements with stop/go thresholds."
    expect_activation: true
  - id: "pos-3"
    input: "Build a consumer-profile rollout strategy with risks, dependencies, and success metrics for hybrid branching."
    expect_activation: true
  - id: "neg-1"
    input: "Enable branch protections now and update workflow files."
    expect_activation: false
  - id: "neg-2"
    input: "Write a React component for a dashboard."
    expect_activation: false

