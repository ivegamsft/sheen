name: "memory-promoter-routing"
description: "Routing evaluation — validates trigger activation for the memory-promoter skill."
skill: "skills/memory-promoter/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Scan these sprint summaries and promote recurring engineering patterns into long-term memory candidates."
    expect_activation: true
  - id: "pos-2"
    input: "Extract repeat fix patterns from recent session transcripts and rank what should be stored."
    expect_activation: true
  - id: "pos-3"
    input: "Create contribution-ready memory payloads from repeated CI troubleshooting learnings."
    expect_activation: true
  - id: "neg-1"
    input: "Implement a new API endpoint for user profile updates."
    expect_activation: false
  - id: "neg-2"
    input: "Deploy Terraform infrastructure to production."
    expect_activation: false
