name: "github-security-posture-routing"
description: "Routing evaluation — validates trigger activation for the github-security-posture skill."
skill: "skills/github-security-posture/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Can you review our GitHub org's branch protection rules and rulesets and flag any gaps?"
    expect_activation: true
  - id: "pos-2"
    input: "We have a backlog of Dependabot alerts — help me triage which ones are critical to fix first."
    expect_activation: true
  - id: "pos-3"
    input: "Generate a GitHub security posture report for our main repository with traffic-light scoring."
    expect_activation: true
  - id: "neg-1"
    input: "There's an SQL injection vulnerability in our login handler — help me fix the code."
    expect_activation: false
  - id: "neg-2"
    input: "How do I sort a list of strings in JavaScript?"
    expect_activation: false
