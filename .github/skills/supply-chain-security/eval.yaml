name: "supply-chain-security-routing"
description: "Routing evaluation — validates trigger activation for the supply-chain-security skill."
skill: "skills/supply-chain-security/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Sign our container image with Cosign as part of the CI/CD pipeline."
    expect_activation: true
  - id: "pos-2"
    input: "Generate a CycloneDX SBOM for our application and add SLSA provenance to our GitHub Actions workflow."
    expect_activation: true
  - id: "pos-3"
    input: "Scan our dependencies and Docker images for known vulnerabilities and verify release artifact integrity."
    expect_activation: true
  - id: "neg-1"
    input: "Add role-based access control to our application so only admins can view the admin panel."
    expect_activation: false
  - id: "neg-2"
    input: "What is the capital of France?"
    expect_activation: false
