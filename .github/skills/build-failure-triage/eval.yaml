name: "build-failure-triage-routing"
description: "Routing evaluation — validates trigger activation for build-failure-triage."
skill: "skills/build-failure-triage/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Our CI build just failed in test stage; help me triage and isolate the root cause."
    expect_activation: true
  - id: "pos-2"
    input: "Classify this pipeline failure and suggest the smallest safe fix sequence."
    expect_activation: true
  - id: "pos-3"
    input: "Create a build failure validation checklist for local and CI confirmation."
    expect_activation: true
  - id: "neg-1"
    input: "Plan product roadmap themes for the next two quarters."
    expect_activation: false
  - id: "neg-2"
    input: "Refactor our auth module to use strategy pattern."
    expect_activation: false
