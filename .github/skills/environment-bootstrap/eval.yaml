name: "environment-bootstrap-routing"
description: "Routing evaluation — validates trigger activation for the environment-bootstrap skill."
skill: "skills/environment-bootstrap/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "How do I set up GitHub Actions OIDC federation with Azure so we don't need stored credentials?"
    expect_activation: true
  - id: "pos-2"
    input: "I need to provision an Azure Storage account for Terraform remote state and configure the backend."
    expect_activation: true
  - id: "pos-3"
    input: "Help me design a dev-to-staging-to-prod promotion workflow with approval gates in GitHub Environments."
    expect_activation: true
  - id: "neg-1"
    input: "Add a new REST endpoint to our Node.js API that handles user profile updates."
    expect_activation: false
  - id: "neg-2"
    input: "What is the difference between a stack and a queue?"
    expect_activation: false
