name: "rca-routing"
description: "Routing evaluation — validates trigger activation for rca skill."
skill: "skills/rca/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "rca: api failure"
    expect_activation: true
  - id: "pos-2"
    input: "Run a root cause analysis on last night's production outage."
    expect_activation: true
  - id: "pos-3"
    input: "Investigate why the asset-health workflow has been failing for 5 weeks."
    expect_activation: true
  - id: "pos-4"
    input: "Trace the contributing factors to the deploy failure and generate prevention recommendations."
    expect_activation: true
  - id: "pos-5"
    input: "rca: capture failing run URL, first failed step, error block, and classify dependency/toolchain/test/env/config before proposing fixes."
    expect_activation: true
  - id: "neg-1"
    input: "Plan the sprint backlog for next quarter."
    expect_activation: false
  - id: "neg-2"
    input: "Refactor the auth module to use OAuth2."
    expect_activation: false
  - id: "neg-3"
    input: "Write unit tests for the new payment service."
    expect_activation: false
