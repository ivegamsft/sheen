name: "project-rules-drift-audit-skill-routing"
description: "Routing evaluation — validates trigger activation for the project-rules-drift-audit skill."
skill: "skills/project-rules-drift-audit/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Run a project rules drift audit in advisory mode against the AIDL guardrail baseline."
    expect_activation: true
  - id: "pos-2"
    input: "Detect missing and modified GitHub Project automation rules compared to the baseline manifest."
    expect_activation: true
  - id: "pos-3"
    input: "Run the project rules drift audit in enforce mode and open issues for high-severity findings."
    expect_activation: true
  - id: "pos-4"
    input: "Generate a remediation rubric for all drifted project automation rules."
    expect_activation: true
  - id: "neg-1"
    input: "Deploy the app to Azure Container Apps."
    expect_activation: false
  - id: "neg-2"
    input: "Write unit tests for the user authentication service."
    expect_activation: false
