name: "entity-framework-migration-routing"
description: "Routing evaluation — validates trigger activation for the entity-framework-migration skill."
skill: "skills/entity-framework-migration/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "We're planning to migrate our data layer from EF6 to EF Core — where should we start?"
    expect_activation: true
  - id: "pos-2"
    input: "How do I refactor our DbContext configuration from EF6 fluent API to EF Core conventions?"
    expect_activation: true
  - id: "pos-3"
    input: "I need to validate that our LINQ queries still behave correctly after migrating to EF Core."
    expect_activation: true
  - id: "neg-1"
    input: "We're starting a new microservice — which ORM should we pick for a greenfield .NET project?"
    expect_activation: false
  - id: "neg-2"
    input: "How do I center a div in CSS?"
    expect_activation: false
