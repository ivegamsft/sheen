name: "performance-aware-design-routing"
description: "Routing evaluation — validates trigger activation for performance-aware-design."
skill: "skills/performance-aware-design/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Assess whether a 1920px hero image with a custom display font will degrade LCP"
    expect_activation: true
  - id: "pos-2"
    input: "Recommend a font loading strategy for the type scale defined in the token file"
    expect_activation: true
  - id: "pos-3"
    input: "Evaluate the motion token duration values for potential INP regressions on mobile"
    expect_activation: true
  - id: "pos-4"
    input: "Score the homepage wireframe spec against Largest Contentful Paint and CLS budgets"
    expect_activation: true
  - id: "neg-1"
    input: "Optimise the Postgres query that fetches product listings for the catalogue page"
    expect_activation: false
  - id: "neg-2"
    input: "Configure horizontal pod autoscaling for the checkout microservice under peak load"
    expect_activation: false