name: "design-system-versioning-routing"
description: "Routing evaluation — validates trigger activation for design-system-versioning."
skill: "skills/design-system-versioning/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Classify renaming --color-brand-blue to --color-action-primary — major, minor, or patch?"
    expect_activation: true
  - id: "pos-2"
    input: "Author a consumer-facing migration guide for upgrading from design system v2 to v3"
    expect_activation: true
  - id: "pos-3"
    input: "Draft a deprecation notice and timeline for removing the legacy elevation tokens"
    expect_activation: true
  - id: "pos-4"
    input: "Generate a CHANGELOG.md entry summarising the semantic token additions in this PR"
    expect_activation: true
  - id: "neg-1"
    input: "Bump the npm package version and cut a GitHub release for the component library"
    expect_activation: false
  - id: "neg-2"
    input: "Decide the REST API versioning strategy for the new user profile endpoints"
    expect_activation: false