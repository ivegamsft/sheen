name: "sheen-onboard-routing"
description: "Routing evaluation — validates trigger activation for sheen-onboard."
skill: "skills/sheen-onboard/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Run the full sheen consumer lifecycle from integration through first agent use for this repo"
    expect_activation: true
  - id: "pos-2"
    input: "Bootstrap a .sheen.yml config, run vendor sync, and build the token output for my project"
    expect_activation: true
  - id: "pos-3"
    input: "Generate a sheen onboarding health report showing token coverage and audit results"
    expect_activation: true
  - id: "pos-4"
    input: "Orchestrate the 5-phase sheen lifecycle: integrate, onboard, inventory, audit, use"
    expect_activation: true
  - id: "neg-1"
    input: "Write the login screen component for the consumer app we are building"
    expect_activation: false
  - id: "neg-2"
    input: "Run a standalone WCAG contrast audit on the semantic token palette"
    expect_activation: false