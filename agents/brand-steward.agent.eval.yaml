name: "brand-steward-routing"
description: "Routing and behavior evaluation for agent brand-steward."
skill: "agents/brand-steward.agent.md"
scenarios:
  - id: "pos-brand-system-refresh"
    input: "Act as brand-steward to reconcile brand identity, logo usage, imagery style, and voice rules for a dashboard redesign."
    expect_activation: true
  - id: "pos-campaign-product-alignment"
    input: "I need one brand decision package that coordinates visual identity, tone guidance, and illustration direction across product and campaign surfaces."
    expect_activation: true
  - id: "pos-brand-risk-triage"
    input: "Review these divergent product mocks for brand consistency, trademark-safe logo placement, and calm familiar messaging before launch."
    expect_activation: true
  - id: "pos-cross-skill-brand"
    input: "Coordinate the brand-identity, brand-voice-tone, logo-usage, and imagery-illustration skills into a single recommendation."
    expect_activation: true
  - id: "neg-token-architecture"
    input: "Define alias token architecture, semantic color slots, and theme fallback rules for a component library."
    expect_activation: false
  - id: "neg-navigation-taxonomy"
    input: "Restructure navigation labels and taxonomy for account settings without changing brand voice or visuals."
    expect_activation: false
  - id: "neg-backend-api"
    input: "Implement REST endpoints, database migrations, and service-layer tests for a billing API."
    expect_activation: false
