# Adopting Tokens

Adoption flow:

1. sync sheen assets into consumer repo
2. map existing styles to semantic roles
3. resolve contrast and completeness gaps
4. generate platform outputs from DTCG source

Avoid bypassing semantic roles with raw color stops.

