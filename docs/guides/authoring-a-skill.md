# Authoring a Skill

Follow `specs/02-skill-contract.spec.md`:

1. create `skills/<name>/`
2. add `SKILL.md` with required frontmatter and sections
3. add `eval.yaml` with >=3 positive and >=2 negative scenarios
4. update catalog/metadata to avoid drift

Use `create-design-skill` when possible.

