# `.sheen.yml` Guide

`.sheen.yml` controls consumer sync behavior:

- `source` and `ref`
- optional allow-lists for `skills`, `agents`, `instructions`, `themes`
- sync overrides (`script`, `exclude`)

Start from `.sheen.yml.example` at repo root.

