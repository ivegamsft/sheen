# Contrast Gate

Contrast checks validate semantic foreground/background pairs per theme:

- light and dark: WCAG AA thresholds
- high-contrast: stricter ratio targets

Current enforcement lives in `scripts/validate-tokens.ps1`.

