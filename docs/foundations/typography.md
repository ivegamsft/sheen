# Typography

Typography is governed as tokenized scale and role mapping:

- family stacks
- weight ramps
- size hierarchy
- line-height and tracking

Use `typography` and `font-mapping` skills for authoring and migration.

