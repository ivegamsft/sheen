# Tokens

Token tiers follow DTCG global/alias semantics:

1. `core/` — context-agnostic primitives (no references)
2. `semantic/` — role/state aliases referencing core
3. `themes/` — overrides for light/dark/high-contrast and brand variants

Validation gates enforce schema, references, completeness, and contrast.

