# Layout & Spacing

Spacing and layout should flow from a single scale and breakpoint contract.

- spacing: tokenized increments
- layout: columns, gutters, container widths
- responsiveness: rules per breakpoint, not ad-hoc exceptions

