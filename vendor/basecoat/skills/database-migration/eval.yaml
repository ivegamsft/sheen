name: "database-migration-routing"
description: "Routing evaluation — validates trigger activation for the database-migration skill."
skill: "skills/database-migration/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Plan an expand-contract migration to rename a column in production without downtime."
    expect_activation: true
  - id: "pos-2"
    input: "Walk me through a blue-green database cutover strategy for our release tomorrow."
    expect_activation: true
  - id: "pos-3"
    input: "Write Flyway versioned migration scripts with undo support for this schema change."
    expect_activation: true
  - id: "neg-1"
    input: "Tune this slow query by adding a covering index — no schema changes needed."
    expect_activation: false
  - id: "neg-2"
    input: "Design the user interface for our admin settings page."
    expect_activation: false
