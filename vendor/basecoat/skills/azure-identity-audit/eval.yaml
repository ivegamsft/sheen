name: "azure-identity-audit-routing"
description: "Routing evaluation — validates trigger activation for the azure-identity-audit skill."
skill: "skills/azure-identity-audit/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Audit these RBAC assignments and managed identity scopes for least privilege."
    expect_activation: true
  - id: "pos-2"
    input: "Review the Entra app registration and federated credentials before we ship."
    expect_activation: true
  - id: "pos-3"
    input: "I need an identity audit for Azure workload federation and privilege scope."
    expect_activation: true
  - id: "neg-1"
    input: "Design the virtual network peering model."
    expect_activation: false
  - id: "neg-2"
    input: "Implement a user sign-in page."
    expect_activation: false
