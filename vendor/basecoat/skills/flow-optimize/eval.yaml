name: "flow-optimize-skill-routing"
description: "Routing evaluation — validates trigger activation for the flow-optimize skill."
skill: "skills/flow-optimize/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Use our audit and suggested issues to create a phased flow optimization roadmap."
    expect_activation: true
  - id: "pos-2"
    input: "Optimize PR throughput by adjusting CI tiers and merge queue policy."
    expect_activation: true
  - id: "pos-3"
    input: "Plan KPI-driven improvements for approval-to-merge time across repos."
    expect_activation: true
  - id: "neg-1"
    input: "Implement dark mode in the web app."
    expect_activation: false
  - id: "neg-2"
    input: "Summarize customer interview notes."
    expect_activation: false

