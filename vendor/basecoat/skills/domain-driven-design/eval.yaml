name: "domain-driven-design-routing"
description: "Routing evaluation — validates trigger activation for the domain-driven-design skill."
skill: "skills/domain-driven-design/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Help me model the bounded contexts and aggregates for our e-commerce domain."
    expect_activation: true
  - id: "pos-2"
    input: "Design the domain events and invariants for our order fulfillment workflow."
    expect_activation: true
  - id: "pos-3"
    input: "How should I apply DDD to define the microservice boundaries for our logistics platform?"
    expect_activation: true
  - id: "neg-1"
    input: "Scaffold a basic CRUD API for managing user profiles with create, read, update, delete."
    expect_activation: false
  - id: "neg-2"
    input: "Fix the CSS padding on the navigation bar so it looks right on mobile."
    expect_activation: false
