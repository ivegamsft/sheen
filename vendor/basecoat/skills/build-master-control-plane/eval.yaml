name: "build-master-control-plane-routing"
description: "Routing evaluation — validates trigger activation for build-master-control-plane."
skill: "skills/build-master-control-plane/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Define lane state transitions for a build master that pauses only impacted lanes."
    expect_activation: true
  - id: "pos-2"
    input: "Create a policy matrix for cloud break-fix eligibility, retry budgets, and auto-revert thresholds."
    expect_activation: true
  - id: "pos-3"
    input: "Design a merge control plane that continues healthy lanes during CI incidents."
    expect_activation: true
  - id: "neg-1"
    input: "Implement a new dashboard widget in React."
    expect_activation: false
  - id: "neg-2"
    input: "Generate a customer announcement email."
    expect_activation: false

