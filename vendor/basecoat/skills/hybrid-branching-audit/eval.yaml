name: "hybrid-branching-audit-routing"
description: "Routing evaluation for hybrid-branching-audit skill activation."
skill: "skills/hybrid-branching-audit/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Audit our repo to see if we are ready for a hybrid branching model with trunk, release, hotfix, and agent lanes."
    expect_activation: true
  - id: "pos-2"
    input: "Create a gap-ranked readiness report for downstream repositories adopting lane-aware branching governance."
    expect_activation: true
  - id: "pos-3"
    input: "Check branch protections, required checks, and agent PR metadata to determine hybrid branch rollout readiness."
    expect_activation: true
  - id: "neg-1"
    input: "Implement the merge lane scheduler workflow and push the changes."
    expect_activation: false
  - id: "neg-2"
    input: "Build a new API endpoint for release analytics."
    expect_activation: false

