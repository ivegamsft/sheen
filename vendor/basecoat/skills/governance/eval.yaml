name: "governance-skill-routing"
description: "Routing evaluation — validates trigger activation for the governance skill."
skill: "skills/governance/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Help me separate common governance rules from repo-specific rules."
    expect_activation: true
  - id: "pos-2"
    input: "I need a canonical label contract and migration map for labels."
    expect_activation: true
  - id: "pos-3"
    input: "Draft the guidance document for shared policy vs local exceptions."
    expect_activation: true
  - id: "neg-1"
    input: "Generate a Dockerfile for a Node app."
    expect_activation: false
  - id: "neg-2"
    input: "Debug this SQL query plan."
    expect_activation: false

