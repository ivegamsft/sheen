name: "ux-routing"
description: "Routing evaluation — validates trigger activation for the ux skill."
skill: "skills/ux/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Map the end-to-end user journey for our onboarding flow from signup through first use."
    expect_activation: true
  - id: "pos-2"
    input: "Create a wireframe spec for the new profile settings screen including all component states."
    expect_activation: true
  - id: "pos-3"
    input: "Run a WCAG accessibility audit on our checkout flow and evaluate the overall usability."
    expect_activation: true
  - id: "neg-1"
    input: "Design the backend infrastructure for our new microservices architecture."
    expect_activation: false
  - id: "neg-2"
    input: "Investigate why our API endpoint has high latency and tune its performance."
    expect_activation: false
