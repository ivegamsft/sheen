name: "api-security-routing"
description: "Routing evaluation — validates trigger activation for the api-security skill."
skill: "skills/api-security/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "How do I secure JWT authentication properly in my REST API?"
    expect_activation: true
  - id: "pos-2"
    input: "I need to add rate limiting to my API endpoints to prevent abuse"
    expect_activation: true
  - id: "pos-3"
    input: "Can you help me prevent SQL injection vulnerabilities in my API request handlers?"
    expect_activation: true
  - id: "neg-1"
    input: "Design the network firewall rules for our on-premises data center"
    expect_activation: false
  - id: "neg-2"
    input: "What is the cheapest Azure VM SKU for a dev workload?"
    expect_activation: false
