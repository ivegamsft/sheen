name: "api-design-routing"
description: "Routing evaluation — validates trigger activation for the api-design skill."
skill: "skills/api-design/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Help me design a REST API contract for our new orders service"
    expect_activation: true
  - id: "pos-2"
    input: "Review this OpenAPI diff and tell me if there are any breaking changes"
    expect_activation: true
  - id: "pos-3"
    input: "We need a versioning strategy for our public API — what are my options?"
    expect_activation: true
  - id: "neg-1"
    input: "Run a penetration test against our staging API endpoints"
    expect_activation: false
  - id: "neg-2"
    input: "How do I center a div in CSS?"
    expect_activation: false
