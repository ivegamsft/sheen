name: "backend-dev-routing"
description: "Routing evaluation — validates trigger activation for the backend-dev skill."
skill: "skills/backend-dev/SKILL.md"
scenarios:
  - id: "pos-1"
    input: "Implement a POST /orders REST endpoint that validates the request body and returns a 201 on success"
    expect_activation: true
  - id: "pos-2"
    input: "Scaffold a service layer for the user management domain with dependency injection"
    expect_activation: true
  - id: "pos-3"
    input: "Add a repository pattern for database access so we can swap out the data store later"
    expect_activation: true
  - id: "neg-1"
    input: "Style the navigation bar component to match the design mockup using Tailwind CSS"
    expect_activation: false
  - id: "neg-2"
    input: "Provision a new Azure SQL database using Terraform"
    expect_activation: false
