name: "orphaned-pr-cleanup-routing"
description: "Routing evaluation — validates trigger activation for the orphaned-pr-cleanup agent."
skill: "agents/basecoat-10-core-orphaned-pr-cleanup.agent.md"
scenarios:
  - id: "pos-1"
    input: "I need help with identifying and resolving stale or orphaned pull requests that have no active owner, blocked reviews, or outdated mergeability status. USE FOR: detect PRs without recent activity, classify close/revive/merge candidates, request ownership reassignment, and produce cleanup actions with SLA windows. DO NOT USE FOR: code implementation work, deep architecture design, or replacing release governance decisions."
    expect_activation: true
  - id: "pos-2"
    input: "Can you handle identifying and resolving stale or orphaned pull requests that have no active owner, blocked reviews, or outdated mergeability status. USE FOR: detect PRs without recent activity, classify close/revive/merge candidates, request ownership reassignment, and produce cleanup actions with SLA windows. DO NOT USE FOR: code implementation work, deep architecture design, or replacing release governance decisions?"
    expect_activation: true
  - id: "pos-3"
    input: "Please use the orphaned-pr-cleanup agent for identifying and resolving stale or orphaned pull requests that have no active owner, blocked reviews, or outdated mergeability status. USE FOR: detect PRs without recent activity, classify close/revive/merge candidates, request ownership reassignment, and produce cleanup actions with SLA windows. DO NOT USE FOR: code implementation work, deep architecture design, or replacing release governance decisions."
    expect_activation: true
  - id: "neg-1"
    input: "code implementation work"
    expect_activation: false
  - id: "neg-2"
    input: "deep architecture design"
    expect_activation: false
