name: "memory-curator-routing"
description: "Routing evaluation — validates trigger activation for the memory-curator agent."
skill: "agents/basecoat-10-core-memory-curator.agent.md"
scenarios:
  - id: "pos-1"
    input: "I need help with extracting, deduplicating, validating, and retrieving cross-session knowledge with the SQLite memory layer, including conflict resolution, decay, and context injection."
    expect_activation: true
  - id: "pos-2"
    input: "Can you handle extracting, deduplicating, validating, and retrieving cross-session knowledge with the SQLite memory layer, including conflict resolution, decay, and context injection?"
    expect_activation: true
  - id: "pos-3"
    input: "Please use the memory-curator agent for extracting, deduplicating, validating, and retrieving cross-session knowledge with the SQLite memory layer, including conflict resolution, decay, and context injection."
    expect_activation: true
  - id: "neg-1"
    input: "Write a haiku about programming."
    expect_activation: false
  - id: "neg-2"
    input: "Give me a recipe for chocolate chip cookies."
    expect_activation: false
