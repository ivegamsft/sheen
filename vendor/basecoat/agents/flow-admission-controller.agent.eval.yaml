name: "flow-admission-controller-routing"
description: "Routing evaluation — validates trigger activation for the flow-admission-controller agent."
skill: "agents/flow-admission-controller.agent.md"
scenarios:
  - id: "pos-1"
    input: "Define WIP limits and merge queue admission rules for all repos."
    expect_activation: true
  - id: "pos-2"
    input: "Design capacity-aware PR intake controls so queue depth stays stable."
    expect_activation: true
  - id: "pos-3"
    input: "Set stale PR and conflict regeneration policy for agent-created branches."
    expect_activation: true
  - id: "neg-1"
    input: "Fix a null reference exception in checkout flow."
    expect_activation: false
  - id: "neg-2"
    input: "Generate social media copy for release day."
    expect_activation: false

