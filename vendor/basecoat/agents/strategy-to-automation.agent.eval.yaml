name: "strategy-to-automation-routing"
description: "Routing evaluation — validates trigger activation for the strategy-to-automation agent."
skill: "agents/basecoat-10-core-strategy-to-automation.agent.md"
scenarios:
  - id: "pos-1"
    input: "I need help with converting manual test paths into automation candidates. Maps paths to smoke tests, regression tiers, or agent specs. ALWAYS files a GitHub Issue for every automation candidate identified."
    expect_activation: true
  - id: "pos-2"
    input: "Can you handle converting manual test paths into automation candidates. Maps paths to smoke tests, regression tiers, or agent specs. ALWAYS files a GitHub Issue for every automation candidate identified?"
    expect_activation: true
  - id: "pos-3"
    input: "Please use the strategy-to-automation agent for converting manual test paths into automation candidates. Maps paths to smoke tests, regression tiers, or agent specs. ALWAYS files a GitHub Issue for every automation candidate identified."
    expect_activation: true
  - id: "neg-1"
    input: "Write a haiku about programming."
    expect_activation: false
  - id: "neg-2"
    input: "Give me a recipe for chocolate chip cookies."
    expect_activation: false
