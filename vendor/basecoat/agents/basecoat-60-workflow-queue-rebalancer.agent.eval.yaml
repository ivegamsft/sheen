name: "basecoat-60-workflow-queue-rebalancer-routing"
description: "Routing evaluation - validates trigger activation for the queue-rebalancer agent."
skill: "agents/basecoat-60-workflow-queue-rebalancer.agent.md"
scenarios:
  - id: "pos-1"
    input: "Use the queue-rebalancer agent to unblock current build breaks by reshuffling dependencies."
    expect_activation: true
  - id: "pos-2"
    input: "Create a blocker-first group from open issues and PRs, cherry-pick minimal fixes, test, and restore normal order."
    expect_activation: true
  - id: "pos-3"
    input: "Route this to queue-rebalancer for fast unblock lane coordination."
    expect_activation: true
  - id: "pos-4"
    input: "Use queue-rebalancer with weighted scoring and high-risk approval check-ins for backlog rebalance recommendations."
    expect_activation: true
  - id: "pos-5"
    input: "Build a hard/soft dependency graph and output feature clusters with confidence and rationale for the rebalance run."
    expect_activation: true
  - id: "pos-6"
    input: "In rebalance mode, return the blocker-critical path derived from dependency chains."
    expect_activation: true
  - id: "neg-1"
    input: "Plan and spec a new feature set for next quarter."
    expect_activation: false
  - id: "neg-2"
    input: "Write a brand-new product redesign proposal."
    expect_activation: false
  - id: "neg-3"
    input: "Estimate team capacity and draft sprint commitments for next month."
    expect_activation: false
