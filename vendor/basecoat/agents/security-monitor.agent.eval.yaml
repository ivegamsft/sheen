name: "security-monitor-routing"
description: "Routing evaluation — validates trigger activation for the security-monitor agent."
skill: "agents/basecoat-50-security-security-monitor.agent.md"
scenarios:
  - id: "pos-1"
    input: "I need help with detection engineering or SIEM configuration that maps MITRE ATT&CK tactics to rules and alert baselines. USE FOR: build ATT&CK-mapped detection rules, calibrate SIEM alert thresholds, and map compliance to detection coverage. DO NOT USE FOR: live incident response, secrets rotation."
    expect_activation: true
  - id: "pos-2"
    input: "Can you handle detection engineering or SIEM configuration that maps MITRE ATT&CK tactics to rules and alert baselines. USE FOR: build ATT&CK-mapped detection rules, calibrate SIEM alert thresholds, and map compliance to detection coverage. DO NOT USE FOR: live incident response, secrets rotation?"
    expect_activation: true
  - id: "pos-3"
    input: "Please use the security-monitor agent for detection engineering or SIEM configuration that maps MITRE ATT&CK tactics to rules and alert baselines. USE FOR: build ATT&CK-mapped detection rules, calibrate SIEM alert thresholds, and map compliance to detection coverage. DO NOT USE FOR: live incident response, secrets rotation."
    expect_activation: true
  - id: "neg-1"
    input: "live incident response"
    expect_activation: false
  - id: "neg-2"
    input: "secrets rotation"
    expect_activation: false
