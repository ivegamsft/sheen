name: "merge-coordinator-routing"
description: "Routing evaluation — validates trigger activation for the merge-coordinator agent."
skill: "agents/basecoat-10-core-merge-coordinator.agent.md"
scenarios:
  - id: "pos-1"
    input: "I need help with multiple feature branches need to be merged into a target branch without interactive git editors hanging automated pipelines. Handles conflict detection, safe resolution, and ordered PR merging."
    expect_activation: true
  - id: "pos-2"
    input: "Can you handle multiple feature branches need to be merged into a target branch without interactive git editors hanging automated pipelines. Handles conflict detection, safe resolution, and ordered PR merging?"
    expect_activation: true
  - id: "pos-3"
    input: "Please use the merge-coordinator agent for multiple feature branches need to be merged into a target branch without interactive git editors hanging automated pipelines. Handles conflict detection, safe resolution, and ordered PR merging."
    expect_activation: true
  - id: "neg-1"
    input: "Write a haiku about programming."
    expect_activation: false
  - id: "neg-2"
    input: "Give me a recipe for chocolate chip cookies."
    expect_activation: false
