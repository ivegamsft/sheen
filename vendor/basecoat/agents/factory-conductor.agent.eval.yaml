name: "factory-conductor-routing"
description: "Routing evaluation — validates trigger activation for the factory-conductor agent."
skill: "agents/basecoat-10-core-factory-conductor.agent.md"
scenarios:
  - id: "pos-1"
    input: "Read .factory-state.json and queue the next ready wave."
    expect_activation: true
  - id: "pos-2"
    input: "Dispatch the next factory workflow based on the shared state file."
    expect_activation: true
  - id: "pos-3"
    input: "Route S2 and S3 work from the current workcell state."
    expect_activation: true
  - id: "neg-1"
    input: "Review this API for security issues."
    expect_activation: false
  - id: "neg-2"
    input: "Summarize a meeting transcript."
    expect_activation: false
