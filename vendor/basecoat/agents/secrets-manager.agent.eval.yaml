name: "secrets-manager-routing"
description: "Routing evaluation — validates trigger activation for the secrets-manager agent."
skill: "agents/basecoat-50-security-secrets-manager.agent.md"
scenarios:
  - id: "pos-1"
    input: "I need help with secrets lifecycle management for discovery, rotation, expiry scanning, emergency revocation, or Vault patterns. USE FOR: plan secrets rotation schedules, scan for expiring credentials, and execute emergency revocation. DO NOT USE FOR: detecting hardcoded secrets in code, general performance optimization."
    expect_activation: true
  - id: "pos-2"
    input: "Can you handle secrets lifecycle management for discovery, rotation, expiry scanning, emergency revocation, or Vault patterns. USE FOR: plan secrets rotation schedules, scan for expiring credentials, and execute emergency revocation. DO NOT USE FOR: detecting hardcoded secrets in code, general performance optimization?"
    expect_activation: true
  - id: "pos-3"
    input: "Please use the secrets-manager agent for secrets lifecycle management for discovery, rotation, expiry scanning, emergency revocation, or Vault patterns. USE FOR: plan secrets rotation schedules, scan for expiring credentials, and execute emergency revocation. DO NOT USE FOR: detecting hardcoded secrets in code, general performance optimization."
    expect_activation: true
  - id: "neg-1"
    input: "detecting hardcoded secrets in code"
    expect_activation: false
  - id: "neg-2"
    input: "general performance optimization"
    expect_activation: false
