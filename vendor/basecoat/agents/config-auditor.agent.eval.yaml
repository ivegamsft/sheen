name: "config-auditor-routing"
description: "Routing evaluation — validates trigger activation for the config-auditor agent."
skill: "agents/basecoat-50-security-config-auditor.agent.md"
scenarios:
  - id: "pos-1"
    input: "I need help with scan a repository for committed or unprotected configuration files that contain secrets, credentials, or sensitive environment-specific values. Produces a findings report with remediation steps."
    expect_activation: true
  - id: "pos-2"
    input: "Can you handle scan a repository for committed or unprotected configuration files that contain secrets, credentials, or sensitive environment-specific values. Produces a findings report with remediation steps?"
    expect_activation: true
  - id: "pos-3"
    input: "Please use the config-auditor agent for scan a repository for committed or unprotected configuration files that contain secrets, credentials, or sensitive environment-specific values. Produces a findings report with remediation steps."
    expect_activation: true
  - id: "neg-1"
    input: "Write a haiku about programming."
    expect_activation: false
  - id: "neg-2"
    input: "Give me a recipe for chocolate chip cookies."
    expect_activation: false
